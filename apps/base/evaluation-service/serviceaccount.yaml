apiVersion: v1
kind: ServiceAccount
metadata:
  name: evaluation-service
  annotations:
    # O overlay substitui pelo ARN devolvido por:
    #   terraform -chdir=terraform/envs/dev/infra output -json irsa_role_arns
    eks.amazonaws.com/role-arn: REPLACE_ME
